package sleeve:platform@0.1.0;

interface audit {
  log: func(event: string);
}

interface settings {
  request-body-limit: func() -> u64;
}

interface lifecycle {
  start: func(invocation: string);
  end: func(invocation: string, trapped: bool);
}
