# sleeve

`sleeve` is experimental middleware built by composing a WebAssembly component in front of an untrusted plugin, where it intercepts imports, applies pluggable policies to neutral call events, and forwards allowed calls to the host. It is under construction.
